spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/finance_dev
    username: finance
    password: finance
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.heitor.finance: DEBUG
    org.hibernate.SQL: DEBUG