{
  "name": "finance-control-api-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress E2E tests for Finance Control API",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:headless": "cypress run --headless --browser electron",
    "test:e2e": "cypress run",
    "pretest:e2e": "node scripts/clean-reports.js",
    "report:merge": "mochawesome-merge \"reports/json/*.json\" -o reports/mochawesome.json",
    "report:generate": "npm run report:merge && marge reports/mochawesome.json --reportDir reports --reportFilename detailed-report --inline",
    "report:summary": "node scripts/generate-summary.js",
    "test:report": "npm run test:e2e && npm run report:generate && npm run report:summary"
  },
  "devDependencies": {
    "ajv": "^8.18.0",
    "cypress": "^13.17.0",
    "cypress-multi-reporters": "^1.6.4",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "typescript": "^5.8.2"
  }
}
